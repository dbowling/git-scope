<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>Git Scope</id>
    <name>Git Scope</name>
    <description>
        1. Adds a tool window with a change browser (similar to Version Control) which shows the current diff of your
        **GIT SCOPE**.
        2. Adapts the Line Status according to your **GIT SCOPE**. Usually this built-in feature shows only the current
        "HEAD" changes.
        3. Adds a Custom *Scope* (inspections, search/replaces, ect) "Git Scope (Files)", which means search results
        will be filtered according to **GIT SCOPE**
    </description>
    <version>3.0.6</version>
    <vendor>WoelkIT</vendor>
    <change-notes>Fixed compatibility issues for latest versions, removed internal API usage, added logo and toolbar icon</change-notes>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>Git4Idea</depends>

    <idea-version since-build="211"/>

    <!-- Plugin's application components (On Start IDE) -->
    <application-components>
        <component>
            <implementation-class>system.OnStart</implementation-class>
        </component>
    </application-components>

    <!-- Plugin's project components -->
    <project-components>
        <component>
            <!-- OnStart Project (with project payload) -->
            <implementation-class>system.MyProjectManagerListener</implementation-class>
        </component>
    </project-components>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">

        <!-- ToolWindow -->
        <toolWindow
                id="Git Scope"
                secondary="true"
                icon="AllIcons.Actions.Diff"
                anchor="left"
                factoryClass="ui.ToolWindowUIFactory"
        />
        <!-- Config (Persistent) -->
        <projectService serviceImplementation="state.State"/>

        <projectService serviceImplementation="implementation.Manager"/>

        <statusBarWidgetProvider implementation="ui.TargetBranchStatusWidgetProvider"/>

    </extensions>

    <actions>
        <action id="ToggleHead" class="system.ToggleHeadAction" text="ToggleHead" description="ToggleHead">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="alt H"/>
        </action>
    </actions>
</idea-plugin>